# Chat-Bot
The Smart Java ChatBot is an interactive desktop-based assistant developed using Core Java. It responds to predefined topics and performs a set of tasks such as answering conceptual questions, accessing external APIs, and interacting with a database using JDBC. The project includes a simple graphical user interface (GUI) for smooth user interaction
